pub enum GameType {
    Ranked,
    Casual,
    Tournament,
}

pub async fn add_player_to_queue(player_id: String, gamemode: GameType) {
    // Add player to queue
}

pub async fn check_player_match(player_id: String) {
    // Check if player has a match
    // ...
}
